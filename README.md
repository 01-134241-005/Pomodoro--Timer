# Pomodoro Daily Timer

A single-screen pomodoro timer with configurable focus and break lengths, audible cycle cues, automatic focus/break transitions, and daily focus-session history that survives reloads.

## Run locally

From the repo root, open `index.html` in any modern browser.

On Windows:

```powershell
start index.html
```

No install step is required.
